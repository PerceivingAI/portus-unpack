# Conversation Archiver CLI

Quickly archive and split conversations from ChatGPT exports or shared URLs.

## Installation

```bash
python -m venv env
source env/bin/activate
pip install -r requirements.txt
